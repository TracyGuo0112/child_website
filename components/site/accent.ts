// Brand accent — single knob for every CTA / highlight. Orange ramp sampled from
// the mascot icon (its ears/headphones are ~#FFA74D); `deep` is #D9730F — bright
// and on-brand while keeping large white-on-fill text at ~3.2:1 (the raw icon
// orange is far too light, ~1.9:1). Mirrors palette's tint/soft/mid/deep.
export const ACCENT = {
  tint: "#FBE6D2",
  soft: "#F2C79A",
  mid: "#E89A3C",
  deep: "#D9730F",
} as const;
