# 喜马拉雅儿童 SDK 网站信息架构优化建议

## 1. 文档目的

本文用于指导现有网站从“营销式长页面”调整为“技术评估型方案站”。

网站的首要任务不是完整展示所有产品和接口信息，而是帮助合作方的技术负责人、产品经理和项目负责人快速回答以下问题：

1. 当前设备和系统是否具备接入条件？
2. 应选择哪一种接入模式？
3. 喜马拉雅提供什么，厂商需要实现什么？
4. 关键数据、账号、内容和播放链路如何运行？
5. 存在哪些硬性限制、技术风险和待确认事项？
6. 接入需要哪些输入、交付物、联调步骤和验收工作？

理想状态下，首次访问者应能在 10 分钟内完成初步技术判断，并把对应专题页面直接分享给嵌入式、App、小程序、服务端等不同研发角色。

## 2. 核心结论

当前网站更像把对外方案文档完整摊开到一个单页中，而不是围绕技术评估任务组织信息。

建议采用“评估首页 + 专题详情页 + 受控完整文档”的三级结构：

- 评估首页：提供结论、关键参数、兼容性判断和整体技术路径。
- 专题详情页：分别说明架构、设备端 SDK、家长端、账号鉴权、运行限制和联调流程。
- 完整技术文档：保留函数签名、枚举、错误码、宏定义和详细时序，继续受控开放。

不建议继续扩大当前单页，也不建议把 v2.6 PDF 中的全部接口说明直接复制到公开页面。

## 3. 当前网站的主要问题

### 3.1 信息顺序不符合技术决策过程

当前顺序大致是：方案概述、整体架构、家长端、玩具端、约束、合作流程。

问题在于：

- “是否适用、资源需求、联网要求、账号要求、不可缓存”等准入条件没有集中展示。
- 硬性约束出现过晚，用户需要阅读大量能力介绍后才能判断是否可接入。
- 商务价值、产品能力、平台架构、接口细节和运营规则混在同一阅读路径中。
- 家长端内容篇幅过长，掩盖了设备端技术评估的主线。

### 3.2 页面过长且内容权重接近

当前桌面首页约 11,400px，手机端约 14,600px。“整体架构”和“家长端能力”占据接近一半长度。

大量内容使用相似的半透明白卡，造成以下问题：

- 每一块内容看起来都同样重要。
- 用户难以区分结论、要求、说明和细节。
- 手机端卡片全部纵向堆叠，形成持续滚动但缺乏阶段感的阅读体验。

### 3.3 公开方案与完整接口文档边界不清

首页存在“语音点播的全部接口”等表述，但实际内容只是能力概览，并非完整接口说明。

网站应清楚区分：

- 公开技术评估信息：帮助判断可行性和工作量。
- 对接说明：解释架构、职责和主要流程。
- 完整技术文档：函数、参数、枚举、错误码和实现细节。

### 3.4 多处技术口径尚未统一

现有首页、FAQ 与《喜马拉雅嵌入式 SDK（C 语言）详解 v2.6》之间存在资源占用、交付方式、AI 职责和缓存含义等差异。发布新版网站前必须先形成唯一可信口径。

## 4. 目标用户与阅读任务

### 4.1 第一用户：技术负责人或架构师

主要任务：

- 判断芯片、系统、网络和播放器是否满足要求。
- 确认 SDK 与现有 Agent、云服务和 App 的边界。
- 识别接入风险和跨团队依赖。
- 估算工作量并决定是否进入技术沟通。

### 4.2 第二用户：嵌入式研发

主要任务：

- 查看 Platform 层需要适配的系统能力。
- 判断 RAM、ROM、线程、HTTP、WebSocket、存储和音频能力。
- 理解初始化、请求、播放、错误回调和退出流程。

### 4.3 第三用户：App、小程序和服务端研发

主要任务：

- 理解用户登录、token、SN、投播和支付链路。
- 确认厂商 App、小程序、厂商服务端与设备之间需要传递的数据。
- 判断哪些能力由喜马拉雅提供，哪些由厂商实现。

### 4.4 第四用户：产品与项目负责人

主要任务：

- 理解方案范围、支持和不支持的能力。
- 确认交付物、联调依赖、验收要求和大致周期。

## 5. 信息组织原则

### 5.1 结论先于解释

每个章节优先回答：

- 是否必须？
- 谁负责？
- 需要什么输入？
- 输出什么结果？
- 不满足时会发生什么？

结论之后再展示原理、流程和详细说明。

### 5.2 评估信息先于能力介绍

页面顺序应先展示兼容性、硬性限制和责任边界，再展示完整能力清单。

### 5.3 按任务拆页，而不是按原始文档章节拆页

页面名称应贴近用户任务，例如“兼容性与系统要求”，而不是“第二章 接入条件”。

### 5.4 渐进披露

- 首页展示关键结论和摘要。
- 专题页展开实现边界和主要流程。
- 完整文档提供函数级细节。

### 5.5 建立唯一可信参数源

资源占用、格式支持、接口责任、交付方式和限制条件不应分别散落在组件和 FAQ 文案中。建议集中维护为结构化数据，再由首页、专题页和 FAQ 复用。

## 6. 推荐的网站结构

### 6.1 一级导航

建议将当前大量章节导航压缩为：

1. 技术概览
2. 兼容性评估
3. 架构与链路
4. 接入指南
5. 高频问题

右侧保留：

- 技术文档
- 申请技术评估

不建议在顶部同时平铺“首页、方案概述、整体架构、家长端能力、玩具端 SDK、约束与数据、合作流程、高频问题、技术文档”等大量入口。

### 6.2 推荐路由

- `/`：技术评估首页
- `/compatibility`：兼容性与系统要求
- `/architecture`：整体架构与职责边界
- `/device-sdk`：设备端 SDK
- `/parent-integration`：家长端与投播接入
- `/auth`：账号、token、SN 与权益鉴权
- `/constraints`：运行、内容与版权约束
- `/integration`：交付、联调与验收
- `/faq`：技术高频问题
- `/docs`：受控完整技术文档

如果暂时不希望增加过多页面，也应至少先拆为：技术评估首页、设备端 SDK、家长端接入、FAQ、技术文档。

## 7. 技术评估首页的推荐结构

### 7.1 首屏：直接说明技术方案

建议标题：

> 喜马拉雅儿童内容接入技术方案

建议副标题：

> 通过设备端 C SDK 与家长端 H5 / 小程序插件，为 AI 玩具提供儿童内容搜索、账号鉴权、流式播放、播放记录与会员能力。

首屏不再以宣传口号为主，应直接展示以下关键事实：

- SDK 语言和参考平台
- 可选接入模式
- RAM / ROM 范围
- HTTP / WebSocket 要求
- 支持的音频格式
- 纯在线与鉴权要求

建议按钮：

- 主按钮：开始兼容性评估
- 次按钮：查看整体架构
- “高频问题”放入辅助链接，不作为首屏主要行动。

### 7.2 接入模式选择

根据 v2.6 文档，当前存在三种功能组合。建议用对比表展示：

| 模式 | ROM | RAM | 基础能力要求 | 适用场景 |
|---|---:|---:|---|---|
| 内容 | 80KB | 70KB | MP3 流播放、HTTP、pthread | 厂商已有语音或 Agent，只需要内容搜索、鉴权和播放 |
| AI Agent | 50KB | 40KB | MP3 流播放、WebSocket、HTTP、pthread | 使用 SDK 提供的 AI 交互能力 |
| 内容 + AI Agent | 110KB | 100KB | MP3 流播放、WebSocket、HTTP、pthread | 同时使用完整内容和 AI 能力 |

必须注明：以上数据不包含本地 UI 图片资源。

如果 AI Agent 功能并未作为儿童 SDK 正式对外开放，则不要公开展示三种模式，应删除对应模式并重新统一资源参数。

### 7.3 兼容性快速检查

建议用“必须满足 / 按功能选配 / 当前不支持”三类清单。

必须满足：

- 具备稳定联网能力。
- 支持 HTTP GET / POST。
- 具备线程、锁、信号量、消息和定时器等基础能力。
- 具备 MP3 流式播放能力。
- 能够存储用户信息、token 和播放断点。
- 能将播放状态回调给 SDK。

按功能选配：

- AI 功能需要 WebSocket。
- SDK 录音链路需要 16kHz、16bit、单声道 PCM。
- 少量内容为 M4A，建议设备同时支持 M4A。
- 带屏设备需要实现相应 UI。

硬性限制：

- 音频 URL 只能用于流式播放。
- 不允许将版权音频持久化缓存到本地。
- 用户身份和权益需要通过喜马拉雅账号体系鉴权。
- 无网络时无法正常请求内容和播放地址。

### 7.4 SDK 分层架构

将 v2.6 的三层设计重绘为适合网页阅读的结构：

1. 厂商应用层：GUI、UI、业务交互。
2. Service Layer：异步线程、消息分发、HTTP 队列、AI 高级接口和播放器封装，可按编译参数裁剪。
3. Core Layer：后台 API、签名与参数处理、用户和设备信息、播放列表及基础工具。
4. Platform Layer：线程、HTTP、WebSocket、设备参数、存储、日志和播放接口抽象。
5. 宿主系统层：厂商实际 OS、网络、音频驱动和播放器实现。

重点说明：

- Core 是 SDK 的最小完备能力。
- Service 是否使用以及使用哪些模块可由接入模式决定。
- Platform 是厂商主要适配区域。
- 当前参考实现基于 Linux，其他平台需要完成对应适配。

### 7.5 双方职责边界

建议采用对照表，而不是连续卡片。

至少包含：

- 系统原语
- HTTP / WebSocket
- 录音与音频驱动
- 流媒体播放器
- 用户与 token 存储
- 搜索与内容服务
- 登录与会员权益
- 播放记录上报
- App / 小程序与设备通信
- UI 实现与验收

统一使用以下标签：

- 喜马拉雅提供
- 厂商实现
- 双方联调
- 按功能选配

### 7.6 关键运行链路

首页只展示四条简化流程，每条控制在 5～8 个节点：

1. SDK 初始化与退出
2. 内容搜索与播放
3. 登录、权益不足与恢复播放
4. 家长端投播到设备

AI Agent 确认属于正式对外范围后，再增加：

5. 录音、WebSocket、ASR、NLU、TTS 与推荐内容播放

详细时序放到专题页。不要直接使用 PDF 中横跨多个参与方、文字极小的原图。

### 7.7 平台适配工作量

将厂商需要完成的适配集中呈现：

1. task：线程、锁、信号量、消息、定时器。
2. HTTP：同步 GET / POST，返回缓冲建议 20KB。
3. AI：录音和 WebSocket，按功能启用。
4. 设备：时间戳、延时、内存、设备参数、日志。
5. 播放：启动、暂停、恢复、停止和状态回调。
6. 存储：用户信息、token、断点和必要业务数据。

每一项都应显示：是否必须、由谁实现、验证方式、对应专题文档。

### 7.8 交付、联调与验收

建议按阶段展示：

1. 确认功能范围和接入模式。
2. 确认芯片、系统、工具链和开发环境。
3. 交付 SDK、参考实现、头文件说明和测试用例。
4. 厂商完成 Platform 适配。
5. 使用 test 目录验证接口稳定性。
6. 完成业务接入与 UI。
7. 双方联调并处理异常场景。
8. 完成功能、UI、数据和合规验收。

在交付方式未统一前，不要公开写“提供源码”“封装库交付”“客户自行编译”或“喜马代编译”等互相冲突的结论。

### 7.9 页面结尾

结尾提供：

- 下载技术评估清单
- 查看技术 FAQ
- 申请技术评估
- 已合作客户进入完整技术文档

## 8. 专题页面建议

### 8.1 兼容性与系统要求

内容包括：

- 三种接入模式及资源参数
- OS 和网络要求
- HTTP 返回缓冲要求
- 录音格式
- 音频格式和解码边界
- 本地存储要求
- 不支持能力
- 评估结果清单

### 8.2 整体架构与职责边界

内容包括：

- SDK 分层架构
- 设备、厂商服务端、App、小程序和喜马服务的关系
- 网络和数据流向
- 双方职责矩阵
- 可选部署或接入模式

### 8.3 设备端 SDK

内容包括：

- 初始化和退出生命周期
- HTTP 异步请求与消息回调模式
- 内容搜索与数据读取
- 播放列表和播放控制
- 播放断点与历史
- token 维护和异常恢复
- 播放数据上报

公开页面只写接口分类、流程和实现要求，不展示全部函数签名。

### 8.4 家长端与投播接入

内容包括：

- H5、原生 App WebView 和微信小程序插件的关系
- 登录、会员领取和续费
- 家长选择内容后如何将专辑 ID、音频 ID 透传到设备
- 喜马不直接与厂商设备通信的边界
- 账号与播放历史如何跨端同步

将现有“组件列表”降为次要内容，不要占据主体篇幅。

### 8.5 账号、token、SN 与权益鉴权

内容包括：

- 用户登录信息如何产生和传递
- access token 与 refresh token 的维护逻辑
- 无屏设备如何保存和清除登录信息
- SN 与会员权益的关系
- 未登录、token 失效、未领取权益、需要支付时的处理方式
- 恢复登录或支付后如何重新播放

### 8.6 运行、内容与版权约束

内容包括：

- 纯在线要求
- 音频不可持久化缓存
- 内容和 URL 的使用边界
- 播放行为数据上报
- 账号输出要求
- 内容运营和品牌规范

### 8.7 交付、联调与验收

内容包括：

- 接入前需要提交的信息
- SDK 与参考实现交付物
- Platform 适配步骤
- 测试目录和验证方式
- 常见错误与日志要求
- 功能、UI、数据和合规验收
- 周期估算的前提条件

## 9. v2.6 PDF 内容如何融入网站

### 9.1 适合公开并重点展示

- SDK 的 Platform / Core / Service 分层架构。
- Service 支持编译参数裁剪。
- 内容、AI Agent、内容 + AI Agent 的资源参数。
- Platform 层适配项目。
- MP3、M4A、HTTP、WebSocket 和录音格式要求。
- 初始化、HTTP 请求、播放和无屏设备的高层流程。
- 流式播放与禁止音频本地缓存的边界。
- 厂商需要保存用户信息和播放断点。
- test 目录验证与最终验收要求。

### 9.2 适合在专题页简化展示

- 应用操作时序。
- AI 连接、录音、ASR、NLU、TTS 时序。
- HTTP 消息队列和回调时序。
- 播放控制和上下曲时序。
- 无屏设备搜索、播放、登录和恢复播放时序。
- 播放数据上报与客户历史记录维护。
- token 刷新和失效处理。

这些流程应重新绘制，而不是截图嵌入 PDF 原图。

### 9.3 只适合完整技术文档

- 全部 C 函数签名。
- 头文件和源文件清单。
- 宏定义、消息 ID 和枚举值。
- 完整错误码。
- ALBUM_PAY_TYPE 和 TRACK_PAY_TYPE 详细枚举。
- 内部参数、密钥名称和实现级配置。

## 10. 必须先统一的技术口径

### 10.1 资源占用

当前首页写 RAM 80KB、ROM 80KB；v2.6 则按功能组合给出：

- 内容：ROM 80KB、RAM 70KB
- AI Agent：ROM 50KB、RAM 40KB
- 内容 + AI Agent：ROM 110KB、RAM 100KB

需要确认最终公开口径、测量条件、是否包含动态内存和是否包含本地 UI 资源。

### 10.2 SDK 交付方式

现有 FAQ 与 v2.6 对以下事项存在差异：

- 是否提供源码。
- 是否由客户自行编译。
- 是否需要客户提供交叉编译工具链和开发环境。
- 是否由喜马根据客户环境封装库后交付。
- 交付渠道是代码仓库还是邮件。

必须由产品、研发和商务共同确认唯一流程。

### 10.3 AI 能力责任

当前网站主要描述厂商负责 ASR 和意图识别，把文本传给喜马进行内容搜索；v2.6 又包含录音、WebSocket、ASR、NLU、TTS 和推荐结果的完整 AI 模块。

需要确认：

- AI Agent 是否属于儿童 SDK 正式对外能力。
- 是否存在“厂商自有 Agent”和“使用喜马 AI Agent”两种模式。
- 两种模式的推荐架构、参数、计费和交付范围。

### 10.4 缓存含义

建议统一表述为：

- 允许为流式播放使用内存 ring buffer。
- 允许保存 token、用户信息和播放断点。
- 不允许将版权音频文件持久化缓存到本地。

避免简单写“SDK 支持缓存”或“内容不可缓存”而不解释缓存对象。

### 10.5 平台支持

建议统一表述为：

> SDK 采用 C 语言并通过 Platform 层抽象系统能力；当前提供 Linux 参考实现，其他 RTOS 或 C 平台需要完成相应适配。

不要直接写成“完全没有系统要求”或“拿到即可在任意芯片运行”。

### 10.6 播放历史归属

需要区分：

- SDK 本地保存的上次播放断点。
- 上报给喜马的播放行为数据。
- 厂商服务端自行保存的用户历史记录。
- 家长端和设备端跨端同步的历史数据。

## 11. 视觉与交互建议

### 11.1 从营销长页转向技术文档布局

- 顶部使用更稳定、克制的导航。
- 桌面端采用左侧目录 + 正文内容。
- 正文列控制在适合连续阅读的宽度。
- 页面顶部显示版本、更新时间和适用范围。
- 保留品牌色，但降低大面积天空背景对正文的干扰。

### 11.2 减少卡片数量

只在以下场景使用强调卡片：

- 关键参数
- 硬性要求
- 风险提示
- 评估结论

普通说明使用正文、表格、定义列表和步骤，不要全部套卡。

### 11.3 统一状态标签

建议使用：

- 必须
- 选配
- 推荐
- 不支持
- 待确认
- 喜马提供
- 厂商实现
- 双方联调

标签的颜色和含义需要全站一致。

### 11.4 时序图和架构图

- 使用网页矢量图或 HTML / CSS 结构重新绘制。
- 桌面端可展示完整流程，移动端切换为分步骤视图。
- 每张图先给出一句结论和参与方说明。
- 不要把文字很小的 PDF 图片直接缩放到手机页面。

### 11.5 移动端

- 避免所有桌面双列内容简单变成无限纵向卡片。
- 兼容性参数使用可横向比较的紧凑表格或模式切换。
- 详细内容使用折叠区域，但关键限制默认展开。
- 移动端目录使用当前章节下拉或页面内目录按钮。
- 锚点跳转后确保标题不会被悬浮导航遮挡。

## 12. 文案调整建议

### 12.1 网站名称

当前“喜马拉雅儿童 SDK 说明文档”容易让人以为首页就是完整接口文档。

建议改为：

> 喜马拉雅儿童内容接入技术方案

### 12.2 章节标题

建议从口号式标题调整为任务式标题：

- “面向 AI 玩具厂商开放内容能力” → “方案范围与适用设备”
- “两个子系统，一套账号体系” → “整体架构与账号关系”
- “管内容、管会员、管续费” → “家长端接入与会员链路”
- “语音点播的全部接口” → “设备端 SDK 核心能力”
- “接入前请确认的硬性约束” → “兼容性要求与运行限制”
- “从资料对接到上线运营” → “交付、联调与验收流程”

### 12.3 风险文案

避免使用“违者后果自负”等不适合公开技术站的措辞。可改为：

> 音频 URL 仅允许用于实时流式播放，不得持久化存储或用于约定范围外的分发；具体要求以合作协议和版权规范为准。

## 13. 内容治理建议

建议建立集中数据源，至少管理以下内容：

- SDK 版本
- 更新时间
- 接入模式
- RAM / ROM 参数
- 系统与网络要求
- 音频格式
- 平台适配项
- 支持和不支持能力
- 双方职责
- 交付物
- 预计周期及前提
- FAQ 关联项

所有页面从同一数据源读取关键参数，避免首页、FAQ 和文档分别维护后发生冲突。

每条公开结论建议标注：

- 信息负责人
- 来源文档
- 生效版本
- 是否允许公开
- 最后确认日期

## 14. 实施优先级

### P0：统一事实口径

在调整页面前完成：

- 确认 AI Agent 是否对外开放。
- 确认三种模式和资源参数。
- 确认 SDK 交付和编译方式。
- 确认缓存、播放历史和数据上报的准确边界。
- 确认哪些参数和流程允许在签约前公开。

### P1：重构信息架构

- 将首页改为技术评估入口。
- 将硬性条件提前。
- 拆分设备端、家长端、鉴权和联调专题。
- 简化顶部导航。

### P2：重做核心表达组件

- 接入模式对比表
- 兼容性检查清单
- SDK 分层架构图
- 双方职责矩阵
- 四条关键时序图
- 风险与限制提示

### P3：完善专题内容

- 补充版本、更新时间和适用范围。
- 增加 FAQ 搜索和分类。
- 建立页面之间的上下文链接。
- 将完整接口细节留在受控文档中。

### P4：视觉优化

- 降低卡片密度。
- 调整正文排版和背景对比度。
- 优化移动端目录、表格和流程图。
- 统一状态标签和术语样式。

## 15. 验收标准

### 15.1 信息完整性

- 用户能在首页找到接入模式、资源要求、系统要求和硬性限制。
- 每项关键能力都能看出由谁负责。
- 公开页面与 FAQ 不再出现相互冲突的参数或结论。

### 15.2 可扫描性

- 技术负责人可在 10 分钟内完成初步兼容性判断。
- 首页不需要读完所有详细内容才能发现不适用条件。
- 关键结论能通过标题、表格和标签快速定位。

### 15.3 可分享性

- 设备端、家长端、鉴权和联调内容拥有独立链接。
- 页面标题能准确表达该页面解决的问题。
- 锚点和章节链接在桌面与手机上均可正常定位。

### 15.4 移动端

- 关键表格和流程图无需缩小到不可读。
- 悬浮导航不遮挡标题。
- 不出现连续十几屏结构相同的卡片堆叠。

### 15.5 内容边界

- 公开页面足以支持技术评估，但不暴露不应公开的实现细节、密钥和完整接口定义。
- 完整技术文档的访问策略与文件真实访问权限一致。

## 16. 给 Claude 的执行要求

1. 先阅读现有页面、FAQ、图表和组件，再提出代码调整方案。
2. 不要把本建议机械地转换成更多卡片或更长的单页。
3. 优先解决信息架构、内容口径和职责边界，再处理颜色、动效和装饰。
4. 对上文列出的口径冲突逐项标记，不要自行猜测业务结论。
5. 如果尚未确认 AI Agent 的对外范围，设计应允许隐藏该模式，而不是把它写死在页面中。
6. 关键参数应集中维护，避免在多个组件中重复硬编码。
7. 复杂 PDF 图应重新绘制为响应式结构，不要直接截图复用。
8. 提交实现前应先给出：
   - 新站点地图
   - 首页内容线框
   - 内容迁移映射
   - 需要业务确认的问题清单
   - 分阶段实施计划
9. 未完成 P0 口径确认前，可以搭建结构和占位状态，但不应把存在争议的参数作为最终公开文案。

